# Copy to .env (do NOT commit secrets)
#
# --- LLM (Azure OpenAI) ---
# Recommended if you want to keep all cloud services under Azure.
AZURE_OPENAI_API_KEY=
# Example: https://<your-resource-name>.openai.azure.com
AZURE_OPENAI_ENDPOINT=
# The *deployment name* you created in Azure OpenAI Studio
AZURE_OPENAI_DEPLOYMENT=
# Usually fine to keep default; change if your resource requires a newer version
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# --- LLM (OpenAI) ---
# Optional alternative.
OPENAI_API_KEY=
# Optional: override API base URL (advanced)
# OPENAI_BASE_URL=
# Optional: override chat model
OPENAI_CHAT_MODEL=gpt-4o-mini

# --- Online TTS (Azure Speech) ---
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=
# Optional: speech-to-text recognition language (used when --stt azure)
AZURE_SPEECH_LANG=en-US
# Optional: override voice
AZURE_SPEECH_VOICE=en-US-JennyNeural
